@model IEnumerable<LibraryManagementSystem.Models.Author>

@{
    ViewData["Title"] = "Authors";
}

<h1>Authors</h1>

@if (User.IsInRole("Admin") || User.IsInRole("Librarian") || User.IsInRole("Staff"))
{
    <p><a asp-action="Create" class="btn btn-primary">Add New Author</a></p>
}

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Bio</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Bio</td>
                <td>
                    @if (User.IsInRole("Admin") || User.IsInRole("Staff"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.AuthorId" class="btn btn-sm btn-secondary">Edit</a>
                    }
                    <a asp-action="Details" asp-route-id="@item.AuthorId" class="btn btn-sm btn-info">Details</a>
                    @if (User.IsInRole("Admin") || User.IsInRole("Staff"))
                    {
                        <a asp-action="Delete" asp-route-id="@item.AuthorId" class="btn btn-sm btn-danger">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>