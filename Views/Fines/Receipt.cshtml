@model LibraryManagementSystem.Models.Fine

@{
    ViewData["Title"] = "Fine Receipt";
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Fine Receipt #@Model.FineId</title>
    <style>
            body {
                font-family: 'Segoe UI', Arial, sans-serif;
                margin: 40px;
                color: #222;
            }

            .receipt-box {
                max-width: 600px;
                margin: auto;
                border: 1px solid #ddd;
                padding: 30px;
                border-radius: 8px;
            }

            .header {
                text-align: center;
                border-bottom: 2px solid #333;
                padding-bottom: 12px;
                margin-bottom: 20px;
            }

                .header h2 {
                    margin: 0;
                }

            .row {
                display: flex;
                justify-content: space-between;
                padding: 6px 0;
                border-bottom: 1px dashed #eee;
            }

            .label {
                color: #666;
            }

            .total {
                font-weight: bold;
                font-size: 1.1em;
                margin-top: 10px;
            }

            .actions {
                text-align: center;
                margin-top: 25px;
            }

            .btn {
                padding: 8px 16px;
                border-radius: 5px;
                text-decoration: none;
                margin: 0 5px;
                border: none;
                cursor: pointer;
            }

            .btn-print {
                background: #198754;
                color: white;
            }

            .btn-back {
                background: #6c757d;
                color: white;
            }

            @@media print {
                .actions {
                    display: none;
                }

                body {
                    margin: 0;
                }

                .receipt-box {
                    border: none;
                }
            }
    </style>
</head>
<body>
    <div class="receipt-box">
        <div class="header">
            <h2>📚 Library Fine Receipt</h2>
            <div>Receipt #@Model.FineId</div>
        </div>

        <div class="row"><span class="label">Book</span><span>@Model.Borrowing?.Book?.Title</span></div>
        <div class="row"><span class="label">Member</span><span>@Model.Borrowing?.Member?.FullName</span></div>
        <div class="row"><span class="label">Due Date</span><span>@Model.Borrowing?.DueDate.ToShortDateString()</span></div>
        <div class="row"><span class="label">Return Date</span><span>@(Model.Borrowing?.ReturnDate?.ToShortDateString() ?? "—")</span></div>
        <div class="row"><span class="label">Status</span><span>@(Model.IsPaid ? "Paid" : "Unpaid")</span></div>
        <div class="row"><span class="label">Paid Date</span><span>@(Model.PaidDate?.ToShortDateString() ?? "—")</span></div>
        <div class="row total"><span class="label">Amount</span><span>৳@Model.Amount</span></div>

        <div class="actions">
            <button class="btn btn-print" onclick="window.print()">⬇ Download / Print PDF</button>
            <a class="btn btn-back" href="javascript:window.close()">Close</a>
        </div>
    </div>
</body>
</html>