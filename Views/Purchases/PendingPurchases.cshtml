@model IEnumerable<LibraryManagementSystem.Models.Purchase>

@{
    ViewData["Title"] = "Pending Purchases";
}

<h1>Pending Purchases</h1>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

@if (!Model.Any())
{
    <p><em>No pending purchases.</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Book</th>
                <th>Member</th>
                <th>Price</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Book?.Title</td>
                    <td>@item.Member?.FullName</td>
                    <td>@("৳" + item.PricePaid.ToString("N2"))</td>
                    <td>
                        <form asp-action="Approve" asp-route-id="@item.PurchaseId" method="post" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-success">Confirm</button>
                        </form>
                        <form asp-action="Reject" asp-route-id="@item.PurchaseId" method="post" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-danger">Reject</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}